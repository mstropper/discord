import React, { FC } from "react";

import { Container, ExpandIcon, Title } from "./styles";

const GuildName: FC = () => {
  return (
    <Container>
      <Title>Matheus Stropper</Title>

      <ExpandIcon />
    </Container>
  );
};

export default GuildName;
